services:
  - type: web
    name: mumbaikarlive
    runtime: python
   startCommand: streamlit run mumbai_transit.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PORT
        value: 10000
